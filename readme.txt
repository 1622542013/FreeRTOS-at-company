2017.6.19 版本
	1，向外发送数据包，在接收arm2串口中断里
	2，向arm2发送数据，在main里。频率随接收arm2数据频率（在接收arm2串口中断里置标志位，在main里判断标志位，然后执行）
	3，无bin格式命令,可使用诺伊滕上位机看数据，姿态等，不可用上位机控制停止，发送和读写标定参数。
		 所有功能命令，均用ascii命令。详见ASCII通信格式说明。
	4，无在线磁标定，可离线磁标定